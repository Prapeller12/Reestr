{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Минимальный набор разрешений главного окна: ядро + открытие файла выгрузки.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
